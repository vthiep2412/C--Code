#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    freopen("MOBILE.inp", "r", stdin);
    freopen("MOBILE.out", "w", stdout);
    
    return 0;
}

