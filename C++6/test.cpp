#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    // freopen(".inp", "r", stdin);
    // freopen(".out", "w", stdout);
    long long a;
    cin>>a;
    cout<<setprecision(2)<<fixed<<(3.14*a*a)/2;
    return 0;
}